Utility for reading U-Boot envrionment on Renesas R-CarH3 based boards

Syntax:
%C [partition to read from] [uboot envrionment variable]

Example:
# uboot-env /dev/mmc2 ethaddr
(Read MAC address from U-Boot envrionment)
# uboot-env /dev/sysdisk2 bootcmd
(Read boot command from U-Boot envrionment)
